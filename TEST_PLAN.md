I will test the users "standard", "locked_out", "problem" and "error" that the system provides according to the time and the required number of tests for this assessment.

I will check:   1. the happy path of a single purchase with the standard user
                2. the happy path of a multiple purchase with the standard user
                3. the list menu at inventory.html with the standard user
                4. the filter by name at inventory.html with the standard user
                5. the filter by price at inventory.html with the standard user
                6. the remove action in cart.html with the standard user
                7. the form validations for the fields to complete in the purchase with the standard user
                8. the displayed message in login page for the locked_out user
                9. the fail in the path of a single purchase with the error user
                10. the repeated images in inventory.html with problem user
                11. login fails with wrong credentials for user/password fields

I will let out of scope performance, glitch or cosmetic tests in order to focus on delivery time.

The area with more risk is definitely the purchase module due it is the main purpose of the app.
Also it is important to check the login for the standard user and menu options displayed for user's basic actions.
