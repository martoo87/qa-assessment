For running tests locally, use VSCode or similar, open the root folder and see the tests list with the UI provided or execute "npx playwright test" in console.
I used AI for generate several comments in the app.
Pipeline shows failure due there is a test designed to fail (see artifact with report to check tests status)
